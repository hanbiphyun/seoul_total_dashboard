import streamlit as st

# ==========================================
# 1. 페이지 설정
# ==========================================
st.set_page_config(
    page_title="서울에서 자취방 구하기 솔루션",
    page_icon="🏘️",
    layout="wide"
)

# ==========================================
# 2. 메인 헤더 및 소개글
# ==========================================
st.title("🏘️ 서울에서 내게 딱 맞는 자취방 구하기")
st.markdown("#### 데이터 기반 서울시 전월세 분석 & 예측 솔루션 🚀")
st.write("원하는 정보가 무엇인가요? 아래의 카드를 클릭하여 원하시는 대시보드로 바로 이동해 보세요.")
st.write("") # 간격 띄우기
st.write("")

# ==========================================
# 3. 3단 수평 배치 (직사각형 카드 디자인)
# ==========================================
# 화면을 3개의 수평 컬럼으로 나눕니다.
col1, col2, col3 = st.columns(3)

# 첫 번째 네모 칸: 현황 분석
with col1:
    # 테두리가 있는 컨테이너 생성
    with st.container(border=True):
        st.subheader("📊 1. 시장 현황 분석")
        st.markdown("**지금 동네 시세는 얼마일까?**")
        st.write(
            "10년 치 서울시 전월세 실거래가 데이터를 바탕으로 "
            "자치구와 법정동별 현재 시세, 보증금/월세 분포, "
            "건물 특성을 한눈에 파악하고 비교해 보세요."
        )
        st.write("") # 버튼 위 여백
        
        # 다른 페이지로 넘어가는 버튼 링크 생성
        st.page_link(
            "pages/1_📊_현황분석_대시보드.py", 
            label="현황 분석 보러가기", 
            icon="🔍"
        )

# 두 번째 네모 칸: 맞춤형 추천
with col2:
    with st.container(border=True):
        st.subheader("💡 2. 맞춤형 매물 추천")
        st.markdown("**나만의 가성비 동네 찾기**")
        st.write(
            "어디서 살아야 할지 막막하신가요? 내 예산(보증금, 월세)과 "
            "원하는 방 크기를 입력하면, AI 기반 추천 알고리즘이 "
            "조건에 딱 맞는 최적의 동네를 추천해 드립니다."
        )
        st.write("") 
        
        st.page_link(
            "pages/2_💡_맞춤형추천_대시보드.py", 
            label="맞춤형 추천 받기", 
            icon="🎯"
        )

# 세 번째 네모 칸: 미래 시세 예측
with col3:
    with st.container(border=True):
        st.subheader("🔮 3. 미래 시세 예측")
        st.markdown("**1년 뒤 내 방 월세는 오를까?**")
        st.write(
            "지금 알아본 방, 1년 뒤엔 시세가 어떻게 될까요? "
            "시계열 딥러닝 예측 모델이 그린 동네별 예상 평당가 지도를 통해 "
            "미래 시세를 예측하고 가성비를 판단하세요."
        )
        st.write("")
        
        st.page_link(
            "pages/3_🔮_미래예측_대시보드.py", 
            label="미래 시세 확인하기", 
            icon="📈"
        )

# ==========================================
# 4. 푸터(하단 꼬리말)
# ==========================================
st.write("")
st.write("")
st.write("")
st.divider()
st.caption("Developed by ESAA OB 1조 🚀 | 서울시 부동산 전월세 실거래가 데이터 활용")
